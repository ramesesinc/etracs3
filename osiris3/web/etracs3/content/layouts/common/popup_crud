<%
	def actions = ANUBIS.getBlock('actions'); 
%>

<a class="rui-dialog-close-action" r:context="${PAGE.context}" r:name="close" r:immediate="true" style="text-decoration:none;">
	<span class="rui-icon rui-icon-close"/> 
</a> 

<table class="master-background rui-dialog" cellpadding="0" cellspacing="0" border="0">
<tr>
	<td class="rui-dialog-content"> ${_content} </td> 
</tr> 
<tr> 
	<td class="rui-dialog-footer-actions">
		<button r:context="${PAGE.context}" r:name="edit"  r:visibleWhen="#{mode=='read'}" r:immediate="true" icon="rui-icon rui-icon-modify">Edit</button>
		<button r:context="${PAGE.context}" r:name="close" r:visibleWhen="#{mode=='read'}" r:immediate="true" icon="rui-icon rui-icon-close">Close</button>
		
		<button r:context="${PAGE.context}" r:name="saveCreate"   r:visibleWhen="#{mode=='create'}" icon="rui-icon rui-icon-save">Save</button>
		<button r:context="${PAGE.context}" r:name="cancelCreate" r:visibleWhen="#{mode=='create'}" icon="rui-icon rui-icon-cancel" r:immediate="true">Cancel</button>
		
		<button r:context="${PAGE.context}" r:name="saveUpdate"   r:visibleWhen="#{mode=='edit'}" icon="rui-icon rui-icon-save">Save</button>
		<button r:context="${PAGE.context}" r:name="cancelUpdate" r:visibleWhen="#{mode=='edit'}" icon="rui-icon rui-icon-cancel" r:immediate="true">Cancel</button>
	</td> 
</tr>
</table>
